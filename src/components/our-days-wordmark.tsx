export function OurDaysWordmark({ className = "" }: { className?: string }) {
  return (
    <span
      className={`our-days-wordmark ${className}`.trim()}
      role="img"
      aria-label="Our Days"
    />
  );
}
